import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;

public class ExampleTest {

    @Test
    public void testExample() {
        assertEquals(128, 64 * 2);
    }
}
